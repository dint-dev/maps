name: example
description: An example.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  cupertino_icons: ^0.1.3
  flutter:
    sdk: flutter
  maps:
    path: ../maps

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true